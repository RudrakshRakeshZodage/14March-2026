Backend:
Navigate to backend/
Run pip install -r requirements.txt
Start the server: python main.py (Server runs on port 8000).
Start the server: uvicorn main:app --reload --host [IP_ADDRESS] --port 8000
Frontend:
Navigate to frontend/
Run npm install
Run npm run dev (Dashboard available at localhost:3000/dashboard).